# Required for AI-generated answers.
# Without this key, the app returns a local extractive answer from the matching text.
OPENAI_API_KEY=

# Optional. Pick any OpenAI model that supports the Responses API in your account.
OPENAI_MODEL=gpt-4.1-mini

# Optional.
PORT=3000
HOST=127.0.0.1
