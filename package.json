{
  "name": "ai-knowledge-assistant",
  "version": "0.1.0",
  "description": "Upload text or PDF files, ask questions, and get answers grounded in saved content.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
